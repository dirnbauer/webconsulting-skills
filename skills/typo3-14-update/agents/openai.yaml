interface:
  display_name: "TYPO3 14 Update"
  short_description: "Update TYPO3 v12/v13 to 14.3 LTS in local DDEV"
  default_prompt: "Use $typo3-14-update to update this TYPO3 v12/v13 project to 14.3 LTS inside the local DDEV environment, keeping rendering identical via the visual regression loop, without deploying."
